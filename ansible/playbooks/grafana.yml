- import_playbook: set_host.yml
- import_playbook: disable_ipv6.yml

- hosts: grafana
  roles:
    - apt_proxy

    - security
    - security-updates

    - docker
    - grafana

    - promtail
    - goss
